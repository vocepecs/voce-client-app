import 'package:flutter/material.dart';

class AudioLinkPage extends StatelessWidget {
  const AudioLinkPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
